pipeline {
    agent any
    stages {
        stage('Building') {
            agent any
            steps {
                sh 'apt-get update; apt-get install iputils-ping; ping google.com'
            }
        }
    }
}
