version: "3.8"
services:
  build-agent:
    image: build-agent:latest
    build:
      context: .
      dockerfile: ./zadanie3_build_agent
  test-agent:
    image: test-agent:latest
    build:
      context: .
      dockerfile: ./zadanie3_test_agent
    depends_on:
      - build-agent
